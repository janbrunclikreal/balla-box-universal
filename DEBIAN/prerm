#!/bin/bash
set -e

TARGET_USER=$(id -nu 1000 2>/dev/null || echo $USER)

echo "[balla_box_1.0.0] Zastavuji služby před odstraněním..."
# Zastavení služby, pokud běží (ignoruje chybu, pokud už stojí)
su - $TARGET_USER -c "systemctl --user stop ballabox.service || true"

exit 0
